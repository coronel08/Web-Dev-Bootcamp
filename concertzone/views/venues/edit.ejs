<% layout('layouts/boilerplate') %> 
<body class="row">
    <h1 class="text-center">Edit Venue</h1>
    <div class="col-10 offset-1">
        <!-- Use method-override to PUT -->
        <form action="/venues/<%=venue._id%>?_method=PUT" method="POST" class="validated-form" enctype="multipart/form-data"  novalidate>
            <div class="row">
                <div class="mb-3 col-md-6">
                    <label class="form-label" for="title">Title</label>
                    <input class="form-control" type="text" id="title" name="venue[title]" value="<%=venue.title%>" required>
                </div>
                <div class="mb-3 col-md-6">
                    <label class="form-label" for="location">Location</label>
                    <input class="form-control" type="text" id="location" name="venue[location]" value="<%=venue.location%>" required>
                </div>
            </div>

            <div class="row">
                <div class="mb-3 col-md-10">
                    <label class="form-label" for="description">Description</label>
                    <textarea class="form-control" name="venue[description]" id="description" type="text" rows="3"><%=venue.description%></textarea>
                </div>
                <div class="mb-2 col-md-2">
                    <label class="form-label" for="price">Price</label>
                    <input class="form-control" type="number" id="price" name="venue[price]" value="<%=venue.price%>" required>
                </div>
            </div>

            <div class="form-file custom-file mb-3">
                <label class="form-file-label custom-file-label" for="image">Add Images</label>
                <input class="form-control form-file-input" type="file" id="image" name="venue[image]" multiple>
            </div>

            <div class="mb-3 row">
                <% venue.images.forEach(function(img, i) { %>
                    <div class="col-md-3 card">
                        <img src="<%= img.url %>" class="img-thumbnail" alt="">
                        <div class="form-check-inline">
                            <label for="image <%= i %>"> Delete?</label>
                            <input type="checkbox" id="image <%= i %>" name="deleteImages[]" value="<%= img.filename %>">
                        </div>
                    </div>
                <% }) %>  
            </div>

            <button class="btn btn-primary">Submit</button>
        </form>
        <a class="btn btn-danger my-3" href="/venues/<%=venue._id%> ">Back</a>
    </div>
</body>
</html>